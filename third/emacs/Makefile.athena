# $Id: Makefile.athena,v 1.5 1997-04-16 17:45:28 ghudson Exp $

SHELL=/bin/sh

prepare:
	rm -f config.cache
	./configure --with-x-toolkit --sharedstatedir=/var/tmp --with-pop \
		--with-hesiod --with-kerberos

clean:
	${MAKE} clean

all:
	${MAKE} all

check:

install:
	${MAKE} install "DESTDIR=$$SRVD"
