This directory contains the following prototype files:

etc.services		An example /etc/services file.
etc.krb.conf		An example /etc/krb.conf file.
etc.krb.realms		An example /etc/krb.realms file which does the 
			correct translation for MIT servers.
prototype.h		Template for .h files.
prototype.c		Template for .c files.
services.append		A portion of /etc/services suitable for appending to
			your version of /etc/services
S70cns			An SVR4 style startup script which goes in /etc/rc2.d
