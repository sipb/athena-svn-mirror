# /* linux */

# Host support files
DES_TIME_SRCS=unix_time.c
DES_TIME_OBJS=unix_time.o
LIB_KRB_HOSTSRCS=unix_glue.c
LIB_KRB_HOSTOBJS=unix_glue.o

TARGET_CDEFS= -DBSD42 -DATHENA -DHAVE_VSPRINTF \
	-DHAS_STRDUP -DWAIT_USES_INT -DNO_YYLINENO -DPOSIX \
	-DNO_FSYNC -DNO_SIGCONTEXT -DHAVE_SIGSET \
	-DNOTTYENT -DNO_LASTLOG -DNO_GETHOSTID -DHAVE_FGETPWENT
# linux has setenv/unsetenv in libc
SETENVSRCS=
SETENVOBJS=
#LEXLIB=
LIBNDBM=-ldbm
RPROGS=-DRPROGS_IN_USR_BIN -D__USE_BSD_SIGNAL
DEPEND=true
