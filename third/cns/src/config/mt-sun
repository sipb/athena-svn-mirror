# /* sunos 4.1 */

# Host support files
DES_TIME_SRCS=unix_time.c
DES_TIME_OBJS=unix_time.o
LIB_KRB_HOSTSRCS=unix_glue.c
LIB_KRB_HOSTOBJS=unix_glue.o

RPROGS=-DRPROGS_IN_USR_UCB -DMOVE_WITH_BCOPY
# /* sunos and solaris */
TARGET_CDEFS= -DBSD42 -DATHENA -DHAVE_VSPRINTF \
	-DHAS_STRDUP -DUIDGID_T -DVARARGS -DNEED_SYS_ERRLIST \
	-DPOSIX_TERMIOS -DHAVE_FGETPWENT -DNO_STRERROR
BSDLIB=$(RESOLV)
