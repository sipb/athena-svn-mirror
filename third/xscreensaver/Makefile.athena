# $Id: Makefile.athena,v 1.1 1998-12-12 22:37:19 ghudson Exp $

SHELL=/bin/sh

prepare:
	rm -f src/config.cache
	./configure --enable-subdir=/usr/athena/libexec/xscreensaver

clean:
	${MAKE} clean

all:
	${MAKE} all

check:

install:
	${MAKE} install DESTDIR="$$SRVD"
