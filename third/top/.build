# $Id: .build,v 1.1 1996-10-11 01:20:54 ghudson Exp $

echo In ${package}: Configuring &&
rm -f .defaults &&
cp defaults.${HOSTTYPE} .defaults &&
module=`sed -n "s/^set module = '\(.*\)'$/\1/p" .defaults` &&
./Configure "$module" &&
echo In ${package}: make clean && make clean &&
echo In ${package}: make all && make all &&
echo In ${package}: make install DESTDIR=$srvd && make install DESTDIR=$srvd
