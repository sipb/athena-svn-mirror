# $Id: Makefile.athena,v 1.2 2005-08-08 17:56:43 ghudson Exp $

dist:

prepare:
	perl Makefile.PL INSTALLDIRS=perl

clean:
	${MAKE} clean

all:
	${MAKE} all

check:

install:
	eval `perl -V:installarchlib`; \
	  rm -rf "$$SRVD/$$installarchlib/perllocal.pod"
	${MAKE} install DESTDIR="$$SRVD"
