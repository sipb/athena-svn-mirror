#
# $Id: Rules.mf,v 1.1.1.1 2001-03-20 18:01:37 ghudson Exp $
#
# Our own Makefile rules
#

#
# We need our own target so that we use -o $@ to get things in the right
# directory
#
.c.o:
	@dir=`dirname $@`; \
		if [ ! -d $$dir ]; then $(MKDIRHIER) $$dir; fi
	$(CC) $(CFLAGS) $(CPPFLAGS) -c $< -o $@
