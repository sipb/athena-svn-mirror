# $Id: Makefile.athena,v 1.18 1999-11-12 16:23:36 ghudson Exp $

SHELL=/bin/sh

prepare:
	rm -f src/config.cache
	cd src && CPPFLAGS=" " ./configure --without-hesiod

clean:
	cd src && ${MAKE} clean

all:
	cd src && ${MAKE} depend all

check:

install:
	cd src && ${MAKE} install DESTDIR="$$SRVD"
