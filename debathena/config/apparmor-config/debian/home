# Note: apparmor fully resolves symlinks before comparing paths
# against these directives, so we don't need to account for either
# CellAliases or non-canonicalized filsys entry paths
@{HOMEDIRS}+=/afs/athena.mit.edu/user/?/?/
@{HOMEDIRS}+=/afs/athena.mit.edu/user/other/
@{HOMEDIRS}+=/afs/sipb.mit.edu/user/
@{HOMEDIRS}+=/afs/net.mit.edu/user/
@{HOMEDIRS}+=/afs/zone.mit.edu/user/
