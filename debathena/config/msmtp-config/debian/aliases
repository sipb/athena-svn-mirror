# This file is used by debathena-msmtp (aka "sendmail") when expanding
# local addresses.
#
# If you're actually trying to run a real mail server, you almost 
# certainly don't want to install debathena-msmtp-{config,mta}.
#
# The root user gets mail from a number of automated processes.
# Specify your MIT address here so that you'll get this mail.
# e.g.
# root: joeuser@mit.edu
root: root

# Mail to other local users should go to root

daemon: root
sys: root
lp: root
mail: root
MAILER-DAEMON: root
www-data: root
syslog: root

