# $Id: Makefile,v 1.2 1998-12-25 19:37:58 ghudson Exp $

SHELL=/bin/sh

all:
	cd os/${OS} && ${MAKE} $@

check:
	cd os/${OS} && ${MAKE} $@

install:
	cd os/${OS} && ${MAKE} $@

clean:
	cd os/${OS} && ${MAKE} $@

distclean:
	cd os/${OS} && ${MAKE} $@
