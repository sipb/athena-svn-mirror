#!/bin/sh
# $Id: Xreset,v 1.2 2001-05-18 21:18:43 zacheiss Exp $

# Xreset
#
# This program is run as root by xlogin to reclaim ownership of the
# devices granted to the user in Xstartup.

cd /dev

# Reclaim the audio devices.
chown root audio audioctl
chmod  600 audio audioctl

# Turn off power saving.  We should be able to do this by starting
# Xsun with the -dpms flag, but that's broken. 
/usr/openwin/bin/xset -display :0.0 -dpms

exit 0
