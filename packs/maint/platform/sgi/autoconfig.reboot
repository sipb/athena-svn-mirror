#!/sbin/sh
#
# /etc/init.d/autoconfig may create a new kernel.  If it does, we want to
# reboot with it.
#

case "$1" in
  'start')
	if [ -f /unix.install ]; then
		reboot
		killall rc2
	fi
        ;;

  'stop')
        ;;

  *)
        echo "usage: autoconfig.reboot {start}"
        ;;
esac

