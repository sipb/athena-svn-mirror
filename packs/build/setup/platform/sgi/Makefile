# $Id: Makefile,v 1.2 1996-10-01 23:59:50 ghudson Exp $

SHELL=/bin/sh

all:

check:

install:
	rm -f ${DESTDIR}/core
	ln -s var/tmp/core.root ${DESTDIR}/core
	rm -f ${DESTDIR}/bin
	ln -s usr/bin ${DESTDIR}/bin

clean:

distclean:
