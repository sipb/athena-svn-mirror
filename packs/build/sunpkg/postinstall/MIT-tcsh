#!/bin/sh

# If /etc/shells exists, make sure it contains tcsh.
if [ -f $PKG_INSTALL_ROOT/etc/shells ]; then
  grep -s '^/bin/athena/tcsh$' $PKG_INSTALL_ROOT/etc/shells > /dev/null || \
    echo "/bin/athena/tcsh" >> $PKG_INSTALL_ROOT/etc/shells
fi

exit 0
