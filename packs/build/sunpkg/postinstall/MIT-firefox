#!/bin/sh

MOZILLA_FIVE_HOME=/usr/athena/lib/firefox

umask 022

# Rebuild the registries.
HOME=/ chroot ${PKG_INSTALL_ROOT:-/} $MOZILLA_FIVE_HOME/build-registries

# Register components and extensions.
HOME=/ chroot ${PKG_INSTALL_ROOT:-/} /usr/athena/bin/firefox -register

exit 0
