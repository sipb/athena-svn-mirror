PKG=MIT-pkgconfig
NAME=A tool for finding compilation flags for libraries, for Athena
DESC=pkgconfig is a tool for determining compilation options. For each required library it reads a configuration file installed in a standard option and ouputs the necessary compiler and linker flags.
