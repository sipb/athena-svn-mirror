# $id:$

create_dir(/usr/athena/config)

FILES=	Imake.rules Imake.tmpl site.def mkdirhier.sh update.conf

install::
	-for i in $(FILES); do \
		install -c -m 0444 $$i $(DESTDIR)/usr/athena/config/; done
