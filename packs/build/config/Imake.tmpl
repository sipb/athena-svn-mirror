/* 
 * Generic Imake Template
 *
 * Future changes:
 * 
 * o Reorganize the structure to employ a config.Imakefile and reduce
 *   the complexity of Imake.tmpl.  Also, the use of site.def should be
 *   re-examined.
 *
 * o Support global project configurations.  This can be done by always
 *   including a Project.tmpl and altering the include path such that
 *   this is found prior to the minimal one in the global templates.
 */

#include <site.def>

/* 
 * WARNING - HACK HACK HACK
 * To allow xmkmf to work with our setup: set CONFDIR to be TOPDIR/config.
 *
 * NEWTOP is still set to . since this is being run from the top of the
 * developer's project tree.  However, TOPDIR is simply the top of the
 * Athena source tree, which contains the Imake configuration files.
 */
#ifdef TOPDIR
#define UseInstalled
#define ConfDir TOPDIR/config
#endif

#ifndef ConfDir
#define ConfDir ${TOP}/config
#endif
#ifndef NEWTOP
#define NEWTOP .
#endif
#ifndef ALibDir
#define ALibDir /usr/athena/lib
#endif
#ifndef ABinDir
#define ABinDir /usr/athena/bin
#endif
#ifndef AIncDir
#define AIncDir /usr/athena/include
#endif
#ifndef AManDir
#define AManDir /usr/athena/man
#endif
#ifndef AEtcDir
#define AEtcDir /usr/athena/etc
#endif
#ifndef AConfDir
#define AConfDir /etc/athena
#endif
#ifndef ARBinDir
#define ARBinDir /bin/athena
#endif
#ifndef AREtcDir
#define AREtcDir /etc/athena
#endif
#ifndef ANewDir
#define ANewDir ABinDir
#endif
#ifndef XAppDefDir
#define XAppDefDir /usr/athena/lib/X11/app-defaults
#endif
#ifndef XBitmapDir
#define XBitmapDir /usr/athena/lib/X11/bitmaps
#endif
#ifndef LintCFlag
#define LintCFlag -o
#endif
#ifndef CcCmd
#define CcCmd cc
#endif
#ifndef LdCmd
#define LdCmd cc
#endif
#ifndef AnsiCcCmd
#define AnsiCcCmd cc
#endif
#ifndef AnsiLdCmd
#define AnsiLdCmd cc
#endif
#ifndef AsCmd
#define AsCmd as
#endif
#ifndef RanLibCmd
#define RanLibCmd ranlib
#endif

#ifndef InstallCmd
#ifdef UseInstalled
#define InstallCmd install -c
#else
#define InstallCmd $(UTILDIR)install -c
#endif
#endif

#ifndef InstPgmFlags
#define InstPgmFlags -s
#endif
#ifndef InstScriptFlags
#define InstScriptFlags -m 755
#endif
#ifndef RmCmd
#define RmCmd rm -f
#endif
#ifndef CcFlags
#define CcFlags
#endif
#ifndef CDebugFlags
#define CDebugFlags -O
#endif
#ifndef ProfCcFlags
#define ProfCcFlags -p
#endif
#ifndef LdFlags
#define LdFlags
#endif
#ifndef LdLibs
#define LdLibs
#endif
#ifndef LintCmd
#define LintCmd lint
#endif
#ifndef LintFlags
#define LintFlags $(DEFINES) -I$(ATHINCDIR)
#endif
#ifndef ShlibExt
#define ShlibExt _s
#endif
#ifndef ShlibDir
#define ShlibDir
#endif

SHELL=/bin/sh
MAKESHELL=/bin/sh


TOP = NEWTOP
DESTDIR=
CONFIGSRC = ConfDir

IRULESRC = $(CONFIGSRC)		
#ifdef UseInstalled
IMAKE= imake
IFLAGS= -I$(IRULESRC) -DTOPDIR=TOPDIR
#else
UTILDIR=$(TOP)/util/bin/
IMAKE= $(UTILDIR)imake
IFLAGS= -I$(IRULESRC)
#endif
ICONFIGFILE = ${IRULESRC}/Imake.tmpl ${IRULESRC}/Imake.rules \
	${IRULESRC}/site.def Imakefile
MKDIRHIER= sh $(IRULESRC)/mkdirhier.sh

ATHLIBDIR=ALibDir
ATHBINDIR=ABinDir
ATHINCDIR=AIncDir
ATHETCDIR=AEtcDir
ATHCONFDIR=AConfDir
ATHMANDIR=AManDir
ATHRBINDIR=ARBinDir
ATHRETCDIR=AREtcDir
ATHNEWDIR=ANewDir

XAPPDEFDIR=XAppDefDir
XBITMAPDIR=XBitmapDir

LINTCFLAG=LintCFlag
#ifdef StdIncDir
LINTFLAGS=$(DEFINES)
#else
LINTFLAGS=$(DEFINES) -I$(ATHINCDIR)
#endif

INSTALL=InstallCmd
RANLIB=RanLibCmd
CC=CcCmd
LD=LdCmd
AS=AsCmd
RM=RmCmd
CDEBUG=CDebugFlags

#ifdef StdIncDir
CFLAGS=$(CDEBUG) CcFlags $(DEFINES)
#else
CFLAGS=$(CDEBUG) CcFlags $(DEFINES) -I$(ATHINCDIR)
#endif

#ifdef StdLibDir
LDFLAGS=LdFlags $(LDDEFS)
#else
LDFLAGS=LdFlags $(LDDEFS) -L$(ATHLIBDIR)
#endif

LDLIBS=LdLibs
SHLIB_EXT=ShlibExt
SHLIB_DIR=ShlibDir

#include "Imake.rules"

all::
install:: install_dirs
install_dirs::

clean::
depend::

FRC::

/**/###########################################################################
/**/# start of Imakefile
#include INCLUDE_IMAKEFILE


makefile_target()

clean::
	rm -f *~ *.o *.a core a.out

#if defined(SUBDIRS) || defined(ISUBDIRS)
/**/###########################################################################
/**/# rules for building in SUBDIRS - do not edit

#ifdef ISUBDIRS
do_subdirs_imakefile(ISUBDIRS)
do_subdirs(ISUBDIRS)
#else
Makefiles::
#endif

#ifdef SUBDIRS
do_subdirs(SUBDIRS)
#endif SUBDIRS

#else
/**/###########################################################################
/**/# empty rules for directories that do not have SUBDIRS - do not edit


Makefiles::

#endif /* if subdirectory rules are needed */

/**/###########################################################################
/**/# dependencies generated by makedepend
