Name: athena-gst-plugins
Summary: GStreamer streaming-media framework plugins, for athena
License: LGPL
Group: Libraries/Multimedia

%description
Contains a set of plugins for GStreamer.

%post
PATH=/usr/athena/bin:$PATH
GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source`
export PATH GCONF_CONFIG_SOURCE
SCHEMAS="gstreamer.schemas"
for S in $SCHEMAS; do
  gconftool-2 --makefile-install-rule /etc/athena/gconf/schemas/$S > /dev/null
done

exit 0
