Name: athena-nvi
Summary: An implementation of the vi editor from Keith Bostic
License: BSD
Group: Applications/Editors

%description
nex/nvi is a freely redistributable implementation of the ex/vi text
editors originally distributed as part of the Fourth Berkeley Software
Distribution (4BSD), by the University of California, Berkeley.

%install
rm -rf $RPM_BUILD_ROOT
mkdir $RPM_BUILD_ROOT
./do.sh -p -s /xxx -d $RPM_BUILD_ROOT install

find $RPM_BUILD_ROOT -type f -or -type l |
  sed -e "s|^$RPM_BUILD_ROOT||" \
      -e "s|^/usr/share/man/.*[0-9]$|\0.gz|" |
  sort > rpm.filelist
