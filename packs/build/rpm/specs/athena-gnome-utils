Name: athena-gnome-utils
Summary: GNOME utility programs, for Athena
License: GPL
Group: Applications/System
Provides: gnome-utils = 2.2.0.3

%description
GNOME (GNU Network Object Model Environment) is a user-friendly set of
GUI applications and desktop tools to be used in conjunction with a
window manager for the X Window System. The gnome-utils package
includes a set of small "desk accessory" utility applications for GNOME
such as a calculator.

%post
PATH=/usr/athena/bin:$PATH
GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source`
export PATH GCONF_CONFIG_SOURCE
SCHEMAS="gdict.schemas gfloppy.schemas logview.schemas"
for S in $SCHEMAS; do
  gconftool-2 --makefile-install-rule /etc/athena/gconf/schemas/$S > /dev/null
done

exit 0
