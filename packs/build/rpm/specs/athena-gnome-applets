Name: athena-gnome-applets
Summary: Small applications for the GNOME panel, for Athena
License: GPL
Group: User Interface/Desktops
Provides: gnome-applets = 2.2.0

%description
GNOME (GNU Network Object Model Environment) is a user-friendly set of
applications and desktop tools to be used in conjunction with a window
manager for the X Window System. The gnome-applets package provides
small utilities for the GNOME panel.

%post
PATH=/usr/athena/bin:$PATH
GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source`
export PATH GCONF_CONFIG_SOURCE
SCHEMAS="battstat.schemas cdplayer.schemas charpick.schemas drivemount.schemas geyes.schemas gkb.schemas gtik.schemas gweather.schemas mini-commander.schemas modemlights.schemas multiload.schemas"
for S in $SCHEMAS; do
  gconftool-2 --makefile-install-rule /etc/athena/gconf/schemas/$S > /dev/null
done

exit 0
