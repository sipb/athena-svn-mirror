Name: athena-gnome-vfs2
Summary: The GNOME virtual file-system libraries, for Athena
License: LGPL
Group: System Environment/Libraries
Provides: gnome-vfs2 = 2.2.0

Requires: athena-glib2 athena-libxml2 athena-GConf2 athena-libbonobo
Requires: athena-bonobo-activation athena-ORBit2 athena-gnome-mime-data
Requires: athena-openssl athena-openafs

%description
GNOME VFS is the GNOME virtual file system. It is the foundation of
the Nautilus file manager.  It provides a modular architecture and
ships with several modules that implement support for file systems,
http, ftp and others.  It provides a URI-based API, a backend
supporting asynchronous file operations, a MIME type manipulation
library and other features.

%post
PATH=/usr/athena/bin:$PATH
GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source`
export PATH GCONF_CONFIG_SOURCE
SCHEMAS="system_http_proxy.schemas"
for S in $SCHEMAS; do
  gconftool-2 --makefile-install-rule /etc/athena/gconf/schemas/$S > /dev/null
done

exit 0
