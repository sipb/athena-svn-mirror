#!/bin/sh

case "$HOSTTYPE" in
sun4)
  echo "SUNWdtbas"					>> "$LINKPACKAGES"
  echo "106934-03"					>> "$PATCHES"
  echo "108219-01"					>> "$PATCHES"
  echo "108374-01"					>> "$PATCHES"
  ;;

sgi)
  rm -f /etc/rc2.d/S98midi /etc/rc2.d/s98midi
  rm -f /etc/rc0.d/K02midi /etc/rc0.d/k02midi
  ln -s ../init.d/midi /etc/rc2.d/S98midi
  ln -s ../init.d/midi /etc/rc0.d/K02midi
  ;;
esac
