#!/bin/sh

case "$HOSTTYPE" in
sun4)
	echo "/etc/mail/sendmail.cf"			>> "$CONFCHG"
	;;

sgi)
	echo "/etc/sendmail.cf"				>> "$CONFCHG"

	# Install patch 3692 locally.
	echo "i patchSG0003692.eoe_sw.base"	>> "$LOCALPACKAGES.patches"
	;;
esac
