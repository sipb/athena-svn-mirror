#!/bin/sh -
# Version-specific update script for 7.7O

case ${MACH} in
DS*)
	echo "/etc/crontab"		>> ${CONFCHG}
        ;;
RSAIX)
        ;;
SUN*)
        echo "NEWUNIX=true"             >> ${CONFVARS}
        ;;
esac
