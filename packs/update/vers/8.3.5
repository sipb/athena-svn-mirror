#!/bin/sh

case $HOSTTYPE in
sun4)
	echo "/etc/mail/sendmail.cf"			>> "$CONFCHG"
	;;

sgi)
	echo "/var/spool/cron/crontabs/root"		>> "$CONFCHG"
	echo "/etc/sendmail.cf"				>> "$CONFCHG"
	touch /var/spool/mqueue/syslog
	chmod 600 /var/spool/mqueue/syslog
	;;
esac
