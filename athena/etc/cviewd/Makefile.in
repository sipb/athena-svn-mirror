# $Id: Makefile.in,v 1.1 1998-09-01 20:54:51 ghudson Exp $

SHELL=/bin/sh
VPATH=@srcdir@
INSTALL=@INSTALL@
INSTALL_PROGRAM=@INSTALL_PROGRAM@
srcdir=@srcdir@
top_srcdir=@top_srcdir@
prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
mandir=@mandir@

CC=@CC@
DEFS=@DEFS@
CPPFLAGS=@CPPFLAGS@
CFLAGS=@CFLAGS@
LDFLAGS=@LDFLAGS@
LIBS=@LIBS@
ALL_CFLAGS=${CPPFLAGS} ${CFLAGS} ${DEFS}

all: cviewd

cviewd: cviewd.o
	${CC} ${LDFLAGS} -o cviewd cviewd.o ${LIBS}

.c.o:
	${CC} -c ${ALL_CFLAGS} $<

check:

install:
	${top_srcdir}/mkinstalldirs ${DESTDIR}${bindir}
	${top_srcdir}/mkinstalldirs ${DESTDIR}${mandir}/man8
	${INSTALL_PROGRAM} cviewd ${DESTDIR}${bindir}
	${INSTALL} -m 444 ${srcdir}/cviewd.8 ${DESTDIR}${mandir}/man8

clean:
	rm -f cviewd.o cviewd

distclean: clean
	rm -f config.cache config.log config.status Makefile
