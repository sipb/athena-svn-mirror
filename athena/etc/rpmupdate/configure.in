dnl Process this file with autoconf to produce a configure script.
AC_INIT(rpmupdate.c)

test -z "$lsbindir" && lsbindir='${sbindir}'
AC_SUBST(lsbindir)

AC_PROG_CC
AC_PROG_INSTALL

AC_OUTPUT(Makefile)
