dnl Process this file with autoconf to produce a configure script.
AC_INIT(saferm.c)

AC_SYS_LARGEFILE

test -z "$lbindir" && lbindir='${bindir}'
AC_SUBST(lbindir)

AC_PROG_CC
AC_PROG_INSTALL

AC_OUTPUT(Makefile)
