# $Id: Makefile.in,v 1.1 2001-05-06 15:53:27 ghudson Exp $

SHELL=/bin/sh
VPATH=@srcdir@
INSTALL=@INSTALL@
INSTALL_PROGRAM=@INSTALL_PROGRAM@
srcdir=@srcdir@
top_srcdir=@top_srcdir@
prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@

all:

check:

install:
	${top_srcdir}/mkinstalldirs ${DESTDIR}${bindir}
	${INSTALL_PROGRAM} revert-to-dash.sh ${DESTDIR}${bindir}/revert-to-dash

clean:

distclean: clean
	rm -f config.cache config.log config.status Makefile
