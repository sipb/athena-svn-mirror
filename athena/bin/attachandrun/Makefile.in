# $Id: Makefile.in,v 1.1 2000-03-15 20:53:10 ghudson Exp $

SHELL=/bin/sh
VPATH=@srcdir@
INSTALL=@INSTALL@
INSTALL_PROGRAM=@INSTALL_PROGRAM@
srcdir=@srcdir@
top_srcdir=@top_srcdir@
prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
mandir=@mandir@

CC=@CC@
DEFS=@DEFS@
CPPFLAGS=@CPPFLAGS@
CFLAGS=@CFLAGS@ ${WARN_CFLAGS} ${ERROR_CFLAGS}
LDFLAGS=@LDFLAGS@
LIBS=@LIBS@
ALL_CFLAGS=${CPPFLAGS} ${CFLAGS} ${DEFS}

all: attachandrun

attachandrun: attachandrun.o
	${CC} ${LDFLAGS} -o attachandrun attachandrun.o -lathdir ${LIBS}

.c.o:
	${CC} -c ${ALL_CFLAGS} $<

check:

install:
	${top_srcdir}/mkinstalldirs ${DESTDIR}${bindir}
	${top_srcdir}/mkinstalldirs ${DESTDIR}${mandir}/man1
	${INSTALL_PROGRAM} attachandrun ${DESTDIR}${bindir}
	${INSTALL} -m 444 ${srcdir}/attachandrun.1 ${DESTDIR}${mandir}/man1

clean:
	rm -f attachandrun.o attachandrun

distclean: clean
	rm -f config.cache config.log config.status Makefile
