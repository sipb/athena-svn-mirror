# $Id: Makefile.in,v 1.1 2001-01-12 18:22:47 ghudson Exp $

SHELL=/bin/sh
VPATH=@srcdir@
INSTALL=@INSTALL@
INSTALL_PROGRAM=@INSTALL_PROGRAM@
srcdir=@srcdir@
top_srcdir=@top_srcdir@
prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
mandir=@mandir@

CC=@CC@
DEFS=@DEFS@
CPPFLAGS=@CPPFLAGS@
CFLAGS=@CFLAGS@
LDFLAGS=@LDFLAGS@
LIBS=`gtk-config --libs`
ALL_CFLAGS=`gtk-config --cflags` ${CPPFLAGS} ${CFLAGS} ${DEFS}

all: panel-wrapper

panel-wrapper: panel-wrapper.o
	${CC} ${LDFLAGS} -o $@ panel-wrapper.o ${LIBS}

.c.o:
	${CC} -c ${ALL_CFLAGS} $<

check:

install:
	${top_srcdir}/mkinstalldirs ${DESTDIR}${bindir}
	${INSTALL_PROGRAM} panel-wrapper ${DESTDIR}${bindir}

clean:
	rm -f panel-wrapper.o panel-wrapper

distclean: clean
	rm -f config.cache config.log config.status Makefile
