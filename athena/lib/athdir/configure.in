dnl Process this file with autoconf to produce a configure script.
AC_INIT(athdir.c)

AC_PROG_CC
AC_PROG_INSTALL
AC_PROG_RANLIB

AC_DEFINE_UNQUOTED(ATHSYS, "$ATHENA_SYS")
AC_DEFINE_UNQUOTED(HOSTTYPE_$HOSTTYPE)

AC_OUTPUT(Makefile)
